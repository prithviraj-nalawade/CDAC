var myData = [];

//operations or calculations

myData.push(10);
myData.push(20);
myData.push(30);


export default myData;

// if any module has to export a single pproperty and that too 
// for the above case, we have to use export default

